
anthropic>=0.40.0
yt-dlp>=2024.0.0
# curl_cffi habilita la "impersonation" de navegador en yt-dlp; sin ella YouTube
# devuelve 403 en el stream de video en muchas redes/IPs. La cota <0.15 es porque
# yt-dlp solo soporta 0.10.x–0.14.x (la 0.15 rompe el backend).
curl_cffi>=0.10,<0.15
streamlit>=1.35.0
requests>=2.31.0
pandas>=2.0.0
faster-whisper>=1.0.0
# OpenCV: detección de caras para decidir el layout del clip (recorte a pantalla
# completa para talking heads vs. plano completo para pantalla compartida).
# Opcional: si falta, el render cae a "fit" (plano completo) sin romper nada.
opencv-python>=4.8.0
# MediaPipe: Face Mesh multi-cara con landmarks de boca. Se usa para detectar
# quién habla (movimiento de labios) y seguir dinámicamente al hablante en el
# recorte 9:16. Opcional: si falta, cae al método Haar (cara más grande, foco fijo).
# 0.10.35+ trae wheels para Python 3.13 y es compatible con numpy 2.
mediapipe>=0.10.35
